"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = require("path");
function getItemNameFromPath(filePath) {
    return path_1.basename(filePath);
}
exports.getItemNameFromPath = getItemNameFromPath;
function getDirectoryName(filePath) {
    return path_1.dirname(filePath);
}
exports.getDirectoryName = getDirectoryName;
function getFileExtension(fileName) {
    let extensionWithDot = path_1.extname(fileName);
    if (extensionWithDot && extensionWithDot.length > 0 && extensionWithDot.charAt(0) === ".") {
        extensionWithDot = extensionWithDot.slice(1);
    }
    return extensionWithDot;
}
exports.getFileExtension = getFileExtension;
function makeHumanReadable(fileName) {
    return fileName
        // insert a space before all caps
        .replace(/([A-Z])/g, " $1")
        // uppercase the first character
        .replace(/^./, (str) => { return str.toUpperCase(); });
}
exports.makeHumanReadable = makeHumanReadable;
//# sourceMappingURL=path-utilities.js.map