"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const filesystem_utilities_1 = require("./filesystem-utilities");
const path_utilities_1 = require("./path-utilities");
const constants_1 = require("../constants");
/**
 * Parses a page or page collection title from the provided source path
 * @param path Path to the source file or directory
 * @returns Page title
 */
function parsePageTitleFromPath(path) {
    let itemName = path_utilities_1.getItemNameFromPath(path);
    const isItemADirectory = filesystem_utilities_1.isDirectory(path);
    if (!isItemADirectory) {
        const fileExtension = path_utilities_1.getFileExtension(itemName);
        itemName = itemName.slice(0, (1 + fileExtension.length) * -1);
        if (isIndexChildPage(itemName)) {
            itemName = itemName.substr(1);
        }
    }
    const humanReadable = path_utilities_1.makeHumanReadable(itemName);
    return humanReadable;
}
exports.parsePageTitleFromPath = parsePageTitleFromPath;
function isIndexChildPage(fileName) {
    return fileName.length > 1 && fileName.charAt(0) === constants_1.PAGE_PREFIX_CHAR;
}
exports.isIndexChildPage = isIndexChildPage;
//# sourceMappingURL=page-utilities.js.map