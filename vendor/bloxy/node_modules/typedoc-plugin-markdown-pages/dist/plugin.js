"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
const components_1 = require("typedoc/dist/lib/output/components");
const converter_1 = require("typedoc/dist/lib/converter/converter");
const events_1 = require("typedoc/dist/lib/output/events");
const constants_1 = require("./constants");
const path_1 = require("path");
const markdown_page_collection_1 = require("./models/markdown-page-collection");
const navigation_helper_1 = require("./helpers/navigation-helper");
const url_mapping_helper_1 = require("./helpers/url-mapping-helper");
const DefaultTheme_1 = require("typedoc/dist/lib/output/themes/DefaultTheme");
const options_helper_1 = require("./helpers/options-helper");
let MarkdownPagesPlugin = class MarkdownPagesPlugin extends components_1.RendererComponent {
    /**
     * Create a new plugin instance.
     */
    initialize() {
        this._optionsHelper = new options_helper_1.OptionsHelper(this.application);
        this._navigationHelper = new navigation_helper_1.NavigationHelper(this._logger);
        this._urlMappingHelper = new url_mapping_helper_1.UrlMappingHelper(this._logger);
        this.listenTo(this.application.converter, {
            [converter_1.Converter.EVENT_RESOLVE_BEGIN]: this._converterResolveBeginEventHandler,
        });
        this.listenTo(this.owner, {
            [events_1.RendererEvent.BEGIN]: this.renderBeginEventHandler,
            [events_1.PageEvent.BEGIN]: this._pageBeginEventHandler,
        });
    }
    get _logger() {
        return this.owner.application.logger;
    }
    _converterResolveBeginEventHandler() {
        this._setupTheme(this.application.renderer);
    }
    /**
     * An event emitted by the Renderer class at the very beginning and ending of the entire rendering process.
     *
     * @param renderer  An event object describing the current render operation.
     */
    renderBeginEventHandler(renderer) {
        this._logger.verbose("Beginning rendering...");
        this._pageCollection = this._getPageCollection();
        // Get a template UrlMapping that will be used to create our own mappings
        const templateMapping = this._urlMappingHelper.getTemplateUrlMapping(renderer.urls);
        // Build UrlMappings for the extra pages that need to be rendered
        const markdownPageMappings = this._urlMappingHelper.createUrlMappings(templateMapping, this._pageCollection);
        renderer.urls.push.apply(renderer.urls, markdownPageMappings);
    }
    /**
     * Triggered before a document will be rendered.
     *
     * @param page An event object describing the current render operation.
     */
    _pageBeginEventHandler(pageEvent) {
        if (this._pageCollection) { // TODO: Make sure this check works if there are no pages
            pageEvent.mdPagesNavigation = this._navigationHelper.getNavigationItem(pageEvent, this._pageCollection, this._optionsHelper.pagesLabel);
        }
    }
    _getPageCollection() {
        try {
            // Get pages and read contents
            const collection = new markdown_page_collection_1.MarkdownPageCollection(this._logger, this._optionsHelper.sourcePath, `${this._optionsHelper.outputDirName}/index.html`);
            collection.title = this._optionsHelper.pagesLabel;
            collection.readContents();
            collection.log();
            return collection;
        }
        catch (e) {
            const errorMessage = `Failed to get page collection. ${e}`;
            this._logger.error(errorMessage);
            throw new Error(errorMessage);
        }
    }
    /**
     * Applies the plugin theme if the user specified its name in options
     * @param renderer Application renderer
     */
    _setupTheme(renderer) {
        const themePath = path_1.join(__dirname, "theme");
        // Setup theme if it was requested
        if (this._optionsHelper.theme === constants_1.THEME_NAME) {
            const pluginTheme = new DefaultTheme_1.DefaultTheme(renderer, themePath);
            renderer.theme = renderer.addComponent("theme", pluginTheme);
        }
    }
};
MarkdownPagesPlugin = __decorate([
    components_1.Component({ name: constants_1.PLUGIN_NAME })
], MarkdownPagesPlugin);
exports.MarkdownPagesPlugin = MarkdownPagesPlugin;
//# sourceMappingURL=plugin.js.map